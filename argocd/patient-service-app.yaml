apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: patient-service

spec:
  project: default

  source:
    repoURL: https://github.com/Sanjaymavi/hackathon-usecase-hcl-main.git
    targetRevision: main
    path: k8s/patient-service

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true